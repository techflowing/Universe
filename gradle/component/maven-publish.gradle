project.ext.gradleComponents.put("maven.publish", { project ->
    project.with {
        apply plugin: 'maven-publish'

        publishing {
            repositories {
                maven {
                    url "${rootDir}/${LOCAL_MAVEN_DIR}"
                }
            }
        }
    }
})