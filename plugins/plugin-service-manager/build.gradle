apply from: rootProject.file("gradle/template/plugin.gradle")

gradlePlugin {
    plugins {
        serviceManagerPlugin {
            id = 'universe.service-manager'
            implementationClass = 'win.techflowing.android.plugin.ServiceManagerPlugin'
        }
    }
}

dependencies {
    implementation project(':plugins:plugin-common')
}